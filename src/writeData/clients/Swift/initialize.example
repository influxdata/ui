import Foundation
import InfluxDBSwift

@main
class Example {
    static func main() {

        let url = "<%= server %>"
        let token = "<INFLUX_TOKEN>"
        let bucket = "<%= bucket %>"
        let org = "<%= org %>"

        let client = InfluxDBClient(url: url, token: token)

    }
}

describe('Member List', () => {
  // set up

  it('can sort by name', () => {
    // in ascending order

    // in descending order    
  })

  it('can sort by role', () => {
    // in ascending order

    // in descending order
  })

  it('can delete a member', () => {

  })
})